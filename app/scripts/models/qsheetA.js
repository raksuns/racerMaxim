/*global define*/

define([
    'backbone'
], function (Backbone) {
    'use strict';
	
    var QSheetModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return QSheetModel;
});
